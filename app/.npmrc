# @circle-fin/developer-controlled-wallets has an optional peer on
# @solana/codecs-strings@^2 while wagmi's Coinbase connector pins v5. We never
# use the SDK's Solana path (settlement is Arc/EVM), so peer resolution is
# relaxed rather than forked. Remove if the Circle SDK bumps its Solana peer.
legacy-peer-deps=true
